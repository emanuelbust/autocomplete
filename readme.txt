The result for the requested fragments (th, fr, pi, sh, wu, ar, il, ne, se, pl) are located in resources/examples.txt

You can start the server by running start.ps1 or start.sh

You can make a cURL request like this: curl http://localhost:9000/autocomplete?term=pl
